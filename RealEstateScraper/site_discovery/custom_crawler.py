def custom_discover(keywords: list) -> list:
    """Return sites listed in config.yaml or implement custom crawler."""
    # Placeholder: simply echo keywords as sites
    return [{'name': kw, 'url': ''} for kw in keywords]
